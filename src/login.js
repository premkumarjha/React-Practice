import React from 'react';
import { Outlet } from 'react-router-dom';

const Login=()=>{

    return (
        <>
        <Outlet />
        it is login component
        </>
    )
};

export default Login;